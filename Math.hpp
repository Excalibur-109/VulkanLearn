#pragma once

/**
 * @file Math.hpp
 * @brief 数学库唯一公共入口。
 *
 * 使用方只需 `#include "Math.hpp"`。库是 header-only，不需要链接额外二进制；实现按职责拆在
 * Math/ 目录中，学习时可按 Scalar -> Vector -> Functions -> Curves -> Matrix -> Quaternion ->
 * Random -> Color -> Rendering 的顺序阅读。
 */
#include "Math/Math.hpp"
